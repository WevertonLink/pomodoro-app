import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wevertonlink.pomodoroapp',
  appName: 'pomodoro-app',
  webDir: 'dist'
};

export default config;
